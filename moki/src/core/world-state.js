export function normalizeWorld(raw){const w=raw&&typeof raw==='object'?raw:{};return {lampOn:w.lampOn!==false,napping:false,waterings:Math.min(999,Math.max(0,Number(w.waterings)||0)),stars:Array.isArray(w.stars)?[...new Set(w.stars.filter(x=>[0,1,2].includes(x)))]:[],discoveries:Array.isArray(w.discoveries)?[...new Set(w.discoveries.filter(x=>typeof x==='string'))].slice(-50):[],lastItem:typeof w.lastItem==='string'?w.lastItem:null};}
export function chapters(s){const count=s.history.filter(h=>h&&!h.legacy).length;const definitions=[{at:0,title:'Наш дом',art:'plant'},{at:3,title:'Уютный уголок',art:'lantern'},{at:9,title:'Окно открытий',art:'telescope'},{at:18,title:'Наши истории',art:'shelf'}];let index=0;for(let i=0;i<definitions.length;i++)if(count>=definitions[i].at)index=i;return {count,index,current:definitions[index],next:definitions[index+1]||null,unlocked:definitions.slice(0,index+1),growth:Math.min(3,1+Math.floor(count/6))};}
export function interactWorld(s,id){if(!s.child.placed.includes(id))return null;s.world=s.world||normalizeWorld();s.world.lastItem=id;if(id==='lantern')s.world.lampOn=!s.world.lampOn;if(id==='bed')s.world.napping=!s.world.napping;if(id==='plant')s.world.waterings=Math.min(999,s.world.waterings+1);return {id,growth:chapters(s).growth,lampOn:s.world.lampOn,napping:s.world.napping};}
export function discoverStar(s,index){if(![0,1,2].includes(index))return false;s.world=s.world||normalizeWorld();if(s.world.stars.includes(index))return false;s.world.stars.push(index);if(s.world.stars.length===3&&!s.world.discoveries.includes('constellation'))s.world.discoveries.push('constellation');return true;}
